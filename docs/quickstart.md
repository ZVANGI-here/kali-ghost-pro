# KALI-GHOST PRO Quickstart v1.1

1. `sudo make install`
2. Run dashboard: `kali-ghost-pro` or Desktop icon
3. Use menu options 1-7 or q to quit
4. Optional: `--dry-run` to test commands without execution
5. Rotate TOR as needed with option 3
6. Encrypt session logs before exit
